<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
	<!-- No UIBackgroundModes: v1 is foreground-first. iOS suspends the app in
	     the background; the core's reconnect loop recovers on return to
	     foreground. No ATS exception either: nostr relays are wss:// and the
	     iroh QUIC/UDP transport is not ATS-governed. -->
	<!-- Quick pair publishes/looks up its PIN rendezvous on the LAN (mDNS) as
	     well as over nostr relays; the LAN half triggers the Local Network
	     prompt on device. It is best-effort — the relays carry the rendezvous
	     regardless (raw multicast is entitlement-gated on hardware). -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>duocb uses the local network to find your other device when quick pairing and to connect devices directly.</string>
</dict>
</plist>
